services:
    taekwonsoftbd_proxy:
        image: nginx:1.27.5-alpine
        build: !reset ""
        volumes: !reset
            - ./proxy/templates:/etc/nginx/templates
            - assets:/assets
        ports: !reset []
        networks:
            - taekwonsoftbd_network
            - pm_public

networks:
    pm_public:
        external: true
